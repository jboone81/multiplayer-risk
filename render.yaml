services:
  - type: web
    name: risk-multiplayer
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /lobby.html
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
